import { DEV_ICONS } from './devIcons';
import { HEADER_SWITCH_ITEMS } from './header-switch-items';
import { PATHS } from './paths';

export const API_URL = 'https://mate.academy/students-api';
export const LOCAL_STORAGE_KEY = 'arr';

export { DEV_ICONS, HEADER_SWITCH_ITEMS, PATHS };
