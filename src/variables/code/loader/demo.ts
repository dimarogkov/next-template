export const LOADER_DEMO_CODE = `import { Loader } from '@/src/components/ui';

const LoaderDemo = () => {
    return (
        <div className='relative flex items-center justify-center w-full h-24'>
            <Loader />
        </div>
    );
};

export default LoaderDemo;`;
