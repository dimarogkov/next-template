export const INPUT_DEMO_CODE = `import { Input, Label } from '@/src/components/ui';

const InputDemo = () => {
    return (
        <Label>
            <Input name='name' placeholder='Username' />
        </Label>
    );
};

export default InputDemo;`;

export const INPUT_PASSWORD_DEMO_CODE = `import { InputPassword, Label } from '@/src/components/ui';

const InputPasswordDemo = () => {
    return (
        <Label>
            <InputPassword name='password' placeholder='Password' />
        </Label>
    );
};

export default InputPasswordDemo;`;
