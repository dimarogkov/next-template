import { Breadcrumb } from '@/src/components/blocks';

const BreadcrumbPreview = () => {
    return <Breadcrumb className='!relative !z-0 !top-auto !p-0 !border-none' />;
};

export default BreadcrumbPreview;
