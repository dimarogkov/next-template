import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Page',
};

const Page = () => {
    return (
        <div>
            <div>Page</div>
        </div>
    );
};

export default Page;
